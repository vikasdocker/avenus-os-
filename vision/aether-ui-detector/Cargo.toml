[package]
name = "aether-ui-detector"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Aether UI element detector: detect buttons, text fields, links, and images from a screen frame. Engine-agnostic — the runtime plugs in a real detector (yolo, layout-parser, etc.) and this crate supplies the typed contract."

[dependencies]
aether-vision-core = { path = "../aether-vision-core" }
aether-ocr = { path = "../aether-ocr" }

[lints]
workspace = true
