[package]
name = "aether-agent-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Aether Agent Core - planning layer + state machine for the Aether autonomous agent (out-of-scope shell for Phase 13)"

[lib]
name = "aether_agent_core"
path = "src/lib.rs"

[dependencies]
aether-core = { path = "../../core/aether-core" }
aether-security = { path = "../../security/aether-security" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
