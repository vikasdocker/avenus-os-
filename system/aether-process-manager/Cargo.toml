[package]
name = "aether-process-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
aether-core = { path = "../../core/aether-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
