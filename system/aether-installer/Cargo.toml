[package]
name = "aether-installer"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Installer abstraction (disk partitioning, GPT+ESP) for Aether OS"

[dependencies]
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
