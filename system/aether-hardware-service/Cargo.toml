[package]
name = "aether-hardware-service"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Aether Hardware Service: the typed model of every piece of hardware Aether can see (CPU, GPU, displays, input, audio, network, storage, battery, thermal, sensors, printers). Pure data + capabilities; the actual HAL lives in the future."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
