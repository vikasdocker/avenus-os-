[package]
name = "aether-command-bar"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Aether AI Command Bar — the prompt surface: mode tabs + text input + submit button, composing the Aether AI-first interaction model."

[dependencies]
aether-design-tokens = { path = "../aether-design-tokens" }
aether-ui-components = { path = "../aether-ui-components" }

[lints]
workspace = true
